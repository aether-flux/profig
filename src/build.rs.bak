fn main() {
    let enabled_features = [
        cfg!(feature="toml"),
        cfg!(feature="json"),
        cfg!(feature="yaml"),
    ];

    let active_count = enabled_featues.iter().filter(|&&x| x).count();

    if active_con
}
